{
  "name": "cf-edge-cron-worker",
  "version": "1.0.0",
  "description": "Cloudflare/EdgeOne Worker scheduled task: poll CF free-tier usage & process external links",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.0"
  }
}
